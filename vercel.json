{
  "buildCommand": "cd \"iit delhi/frontend\" && npm install && npm run build",
  "outputDirectory": "iit delhi/frontend/dist",
  "framework": "vite",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
